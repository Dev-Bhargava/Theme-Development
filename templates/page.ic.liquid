<div class="container">
  <p>Let's take a look!</p>
  <div class="row">
    <div class="input-group mb-3">
      <span for="name" class="input-group-text" id="basic-addon1">Investing Amount: </span>
      <input id="amount" type="text" class="form-control mr-5" placeholder="Username" aria-label="Username" aria-describedby="basic-addon1">

      <span for="name" class="input-group-text" id="basic-addon1">Months: </span>
      <input id="months" type="text" class="form-control mr-5" placeholder="Username" aria-label="Username" aria-describedby="basic-addon1">

      <span for="name" class="input-group-text" id="basic-addon1">ROI/Month: </span>
      <input id="roi" type="text" class="form-control mr-5" placeholder="Username" aria-label="Username" aria-describedby="basic-addon1">
    </div>
  </div>
  <div class="col-12">
    <button id="submit" type="submit" class="btn btn-primary">Calculate</button>
    <button id="clear" type="submit" class="btn btn-primary">Clear</button>
  </div>

  <p>Invested Amount is <span id="outputName">____</span>. The city in which you live is <span id="outputCity">____</span>. </p>
</div>

    <script>
    var submit = document.getElementById("submit");
    var capitalize = document.getElementById("capitalize");
    var clear = document.getElementById("clear");

    submit.onclick = function(){
    	var amount = document.getElementById("amount").value;
      var months = document.getElementById("months").value;

      document.getElementById("outputName").innerText = amount;
      document.getElementById("outputCity").innerText = months;

      var pa = parseInt(amount);
      var month = months;
      var transaction = 4;
      var transaction_count = 0;
      document.write('Total Invested Amount : ' + pa +'<br/><br/><br/>');
        for (var i = 1; i <= months; i++) {

          document.write('<strong>'+ i + ' Month<br/>' + '----------------------------------------------------</strong>' + '<br/>');
          for (var j = 0; j < transaction; j++) {
            var single_roi = pa/100*5;
            var total_amt =  single_roi + pa;
            document.write("Week " + transaction_count + "-->  " + total_amt.toFixed(2) +" (" + single_roi.toFixed(2) + ')<br/>');
            transaction_count++;
            pa = total_amt;
          }
          document.write('---------------------------------------------------- <br/>');
        }
    }

    clear.onclick = function(){
    	document.getElementById("amount").value = "";
      document.getElementById("months").value = "";
    }
  </script>
